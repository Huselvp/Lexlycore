spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/lexly_database
    username: postgres
    password: password
  driverClassName: org.postgresql.Driver
  sql:
    init:
      platform: postgres

server:
  port: 8081
